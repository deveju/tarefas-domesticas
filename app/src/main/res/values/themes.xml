<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DomeTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/marrom_medio</item>
    </style>

    <style name="activityTheme" parent="Base.Theme.DomeTask">
        <item name="android:textViewStyle">@style/myTextViewStyle</item>
    </style>

    <style name="myTextViewStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="Theme.DomeTask" parent="Base.Theme.DomeTask" />
    <style name="Theme.DomeTask.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomActionBarTheme" parent="Base.Theme.DomeTask">
        <!-- action bar apenas da onboarding activity -->
        <item name="colorPrimary">#0c091c</item>
    </style>

    <style name="Theme.DomeTask.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.DomeTask.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>